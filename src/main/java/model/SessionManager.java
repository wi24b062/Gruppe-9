package model;

public class SessionManager {
    public static ChargingSession startSession(String customerId, String pointId, InMemoryRepo repo) {
        ChargingSession o = null;
        return o;
    }
}
